---
title: "如何使用和定制这个博客"
description: "详细介绍如何配置、定制和部署这个 Astro 博客的完整指南。"
pubDatetime: 2024-01-02T00:00:00Z
modDatetime: 2024-01-03T00:00:00Z
author: "hepingfly"
tags: ["Astro", "教程", "博客"]
categories: ["教程"]
featured: true
draft: false
---

## 目录

这篇文章将详细介绍如何使用和定制这个博客系统。

## 快速开始

### 1. 安装依赖

```bash
# 使用 pnpm（推荐）
pnpm install

# 或使用 npm
npm install

# 或使用 yarn
yarn install
```

### 2. 开发模式

```bash
pnpm dev
```

访问 `http://localhost:4321` 查看效果。

### 3. 构建生产版本

```bash
pnpm build
```

## 核心配置

### 网站配置

编辑 `src/config/site.config.ts`：

```typescript
export const SITE = {
  website: "https://blog.hepingfly.com", // 你的域名
  author: "你的名字",
  desc: "你的博客描述",
  title: "你的博客标题",
  // ... 其他配置
};
```

### 社交媒体配置

编辑 `src/config/social.config.ts`：

```typescript
export const SOCIALS: SocialPlatform[] = [
  {
    name: "Github",
    href: "https://github.com/your-username",
    active: true,
    // ... 其他配置
  },
  // 添加更多平台
];
```

**新增平台的步骤：**

1. 在 `SOCIALS` 数组中添加配置
2. 如需自定义图标，在 `src/components/SocialIcon.astro` 中添加 SVG
3. 无需修改页面代码，配置会自动生效

### SEO 配置

编辑 `src/config/seo.config.ts`：

```typescript
export const TWITTER_CONFIG = {
  card: "summary_large_image",
  site: "@your-twitter", // 你的 Twitter
  creator: "@your-twitter",
};
```

## 写文章

### 创建新文章

在 `src/content/blog/` 目录下创建新的 `.md` 或 `.mdx` 文件：

```markdown
---
title: "文章标题"
description: "文章描述/摘要"
pubDatetime: 2024-01-01T00:00:00Z
modDatetime: 2024-01-02T00:00:00Z  # 可选
author: "作者名"
tags: ["标签1", "标签2"]
categories: ["分类1"]
featured: false  # 是否为精选文章
draft: false     # 是否为草稿
---

文章内容...
```

### Frontmatter 字段说明

| 字段 | 必需 | 说明 |
|------|------|------|
| `title` | ✅ | 文章标题 |
| `description` | ✅ | 文章描述（用于 SEO 和摘要） |
| `pubDatetime` | ✅ | 发布时间 |
| `modDatetime` | ❌ | 修改时间 |
| `author` | ❌ | 作者（默认使用站点配置） |
| `tags` | ❌ | 标签数组 |
| `categories` | ❌ | 分类数组 |
| `featured` | ❌ | 是否为精选（默认 false） |
| `draft` | ❌ | 是否为草稿（默认 false） |
| `ogImage` | ❌ | 自定义 OG 图片 |
| `canonicalURL` | ❌ | 规范链接 |

### 图片处理

**方式一：使用 public 目录**

```markdown
![图片描述](/images/my-image.jpg)
```

**方式二：使用相对路径（推荐）**

```markdown
![图片描述](./images/my-image.jpg)
```

## 定制功能

### 修改打赏二维码

1. 准备你的二维码图片（推荐 300x300 像素）
2. 放到 `public/donate-qrcodes/` 目录：
   - `wechat.png` - 微信赞赏码
   - `alipay.png` - 支付宝收款码
3. 无需修改代码，自动生效

### 禁用打赏功能

编辑 `src/layouts/PostLayout.astro`，注释掉打赏组件：

```astro
<!-- 打赏组件 - 自动插入到文章底部 -->
<!-- <div class="post-donate">
  <Donate />
</div> -->
```

### 自定义主题颜色

编辑 `src/styles/global.css`：

```css
:root {
  --color-accent: 79, 70, 229; /* 主题色（RGB 值）*/
  /* 修改为你喜欢的颜色 */
}
```

### 添加导航菜单

编辑 `src/components/Header.astro`：

```typescript
const navItems = [
  { name: "首页", href: "/" },
  { name: "博客", href: "/blog" },
  { name: "标签", href: "/tags" },
  { name: "关于", href: "/about" },
  { name: "新页面", href: "/new-page" }, // 添加新菜单
];
```

## 部署到 GitHub Pages

### 1. 配置仓库

在 GitHub 仓库设置中：

1. 进入 `Settings` > `Pages`
2. Source 选择 `GitHub Actions`

### 2. 推送代码

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

GitHub Actions 会自动构建和部署。

### 3. 配置自定义域名

**在仓库中添加 CNAME 文件：**

```bash
echo "blog.hepingfly.com" > public/CNAME
```

**在域名 DNS 设置中添加记录：**

```
类型：CNAME
名称：blog
值：your-username.github.io
```

## 最佳实践

### 写作建议

1. **标题要清晰**：让读者一眼知道文章内容
2. **善用标签**：每篇文章 2-5 个标签
3. **添加目录**：长文章使用 `## 目录` 自动生成
4. **代码高亮**：指定语言获得更好的高亮效果

### 性能优化

1. **图片优化**：使用 WebP 格式，压缩图片大小
2. **懒加载**：使用 `loading="lazy"` 属性
3. **代码分割**：Astro 自动处理

### SEO 最佳实践

1. **每篇文章都有唯一的 title 和 description**
2. **使用语义化的标题层级**（h1 > h2 > h3）
3. **添加 alt 文本到图片**
4. **内部链接**：在文章间相互链接
5. **定期更新内容**：使用 `modDatetime` 标记更新

## 故障排除

### 构建失败

**检查 frontmatter 格式：**

```bash
pnpm astro check
```

**常见问题：**

- 日期格式错误（必须是 ISO 8601 格式）
- 缺少必需字段（title, description, pubDatetime）
- Markdown 语法错误

### 样式不生效

1. 检查 Tailwind 类名是否正确
2. 运行 `pnpm dev` 重新编译
3. 清除缓存：删除 `.astro` 和 `node_modules/.astro` 目录

### 图片不显示

1. 检查路径是否正确
2. 确保图片在 `public` 目录或相对路径正确
3. 检查文件扩展名大小写

## 进阶定制

### 添加评论系统

可以集成 Giscus、Disqus 等评论系统。

### 添加站点统计

推荐使用：
- Google Analytics
- Umami（开源）
- Plausible（注重隐私）

### 添加搜索功能

博客已集成 Fuse.js，可以实现客户端搜索。

## 获取帮助

遇到问题？

1. 查看 [Astro 官方文档](https://astro.build)
2. 查看本项目的 README.md
3. 在 GitHub Issues 提问

## 总结

这个博客系统提供了：

✅ 完整的 SEO 优化
✅ 灵活的配置系统
✅ 优雅的打赏功能
✅ 社交媒体集成
✅ 自动化部署
✅ 优秀的性能

开始你的写作之旅吧！🚀
